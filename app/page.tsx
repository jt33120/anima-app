import Univers from "./_scene/univers";

// Prototype d'univers immersif (moteur WebGL, spike vers AD-7).
// Défile pour plonger dans la profondeur. Les vrais calques peints (Gemini) viendront s'y composer.
export default function Page() {
  return <Univers />;
}
